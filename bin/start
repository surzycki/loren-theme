#!/bin/bash

docker-compose -f docker-compose.yml up --build --detach
docker-compose exec dev yarn install
docker-compose exec dev shopify login --store piu-test.myshopify.com
docker-compose exec dev yarn start
