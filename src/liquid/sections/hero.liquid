{% render 'hero' with {
    product: all_products[section.settings.product]
} %}

{% schema %}
                {
                  "name": "Hero",
                  "settings":[
                    {
                      "type": "product",
                      "id": "product",
                      "label": "Product"
                    }
                  ],
                  "presets": [
                    {
                      "name": "Hero",
                      "category": "Landing Page"
                    }
                  ]
                }
{% endschema %}
