{% render: 'engagements' with {
    settings: section.settings,
    id: section.id
} %}

{% schema %}
                {
                  "name": "Engagements",
                  "settings":[
                    {
                      "type": "image_picker",
                      "id": "engagement_image",
                      "label": "Engagement Image"
                    }
                  ],
                  "presets": [
                    {
                      "name": "Engagements",
                      "category": "Landing Page"
                    }
                  ]
                }
{% endschema %}
